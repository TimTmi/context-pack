interface:
  display_name: "Use Context"
  short_description: "Read and use the project .context pack efficiently."
  default_prompt: "Use the project context files to answer or implement accurately without loading unnecessary context."
